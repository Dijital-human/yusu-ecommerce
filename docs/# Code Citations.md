# Code Citations

## License: bilinmiyor
https://github.com/Rart3x/42-transcendence/blob/c5262382247e3b8a0ff635e6fdbbfb65fbde4f7d/srcs/requirements/front/src/components/History.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: MIT
https://github.com/verde-mining/verde-mining-website/blob/18d4060e56af1c318bbc97625596435b7aad47fc/pages/status/index.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/siddharthd0/siddharthduggal.com/blob/7752be6b17f03c0e62b9c9d6bda56e5b31cf2086/new/my-app/src/pages/index.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/Cocodrilette/eth-block-explorer/blob/86c2a7b6455051f1b97fc8d1216024016cdfa46c/src/lib/utils.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/nickgabriadze/full-stack-todo-application/blob/70888de7bcc19f694b40e79f9e21da0c08f174e3/src/components/personal/components/showTodos/timeDiff.ts

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: MIT
https://github.com/verde-mining/verde-mining-website/blob/18d4060e56af1c318bbc97625596435b7aad47fc/pages/status/index.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (
```


## License: bilinmiyor
https://github.com/siddharthd0/siddharthduggal.com/blob/7752be6b17f03c0e62b9c9d6bda56e5b31cf2086/new/my-app/src/pages/index.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (
```


## License: bilinmiyor
https://github.com/Cocodrilette/eth-block-explorer/blob/86c2a7b6455051f1b97fc8d1216024016cdfa46c/src/lib/utils.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (
```


## License: bilinmiyor
https://github.com/nickgabriadze/full-stack-todo-application/blob/70888de7bcc19f694b40e79f9e21da0c08f174e3/src/components/personal/components/showTodos/timeDiff.ts

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (
```


## License: bilinmiyor
https://github.com/Rart3x/42-transcendence/blob/c5262382247e3b8a0ff635e6fdbbfb65fbde4f7d/srcs/requirements/front/src/components/History.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (
```


## License: bilinmiyor
https://github.com/siddharthd0/siddharthduggal.com/blob/7752be6b17f03c0e62b9c9d6bda56e5b31cf2086/new/my-app/src/pages/index.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/Cocodrilette/eth-block-explorer/blob/86c2a7b6455051f1b97fc8d1216024016cdfa46c/src/lib/utils.js

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/nickgabriadze/full-stack-todo-application/blob/70888de7bcc19f694b40e79f9e21da0c08f174e3/src/components/personal/components/showTodos/timeDiff.ts

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: bilinmiyor
https://github.com/Rart3x/42-transcendence/blob/c5262382247e3b8a0ff635e6fdbbfb65fbde4f7d/srcs/requirements/front/src/components/History.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```


## License: MIT
https://github.com/verde-mining/verde-mining-website/blob/18d4060e56af1c318bbc97625596435b7aad47fc/pages/status/index.vue

```
);
  const diff = now.getTime() - past.getTime();
  
  const seconds = Math.floor(diff / 1000);
  const minutes = Math.floor(seconds / 60);
  const hours = Math.floor(minutes / 60);
  const days = Math.floor(hours / 24);
  
  if (seconds < 60) return `${seconds} seconds ago`;
  if (minutes < 60) return `${minutes} minutes ago`;
  if (hours < 24) return `${hours} hours ago`;
  if (days < 7) return `${days
```

